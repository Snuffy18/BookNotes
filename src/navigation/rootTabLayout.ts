/**
 * Bottom padding for main ScrollViews on root tabs (Scan, Library, book reports, Profile)
 * so the last card stays clear of the floating tab pill (incl. ~16px breathing room past the pill).
 */
export const ROOT_TAB_MAIN_SCROLL_BOTTOM_PADDING = 120;
