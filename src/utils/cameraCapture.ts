/** Shared options for in-app camera captures (custom sound effect replaces system shutter). */
export const CAMERA_PICTURE_OPTIONS = {
  quality: 0.9,
  skipProcessing: true,
  shutterSound: false,
} as const;
